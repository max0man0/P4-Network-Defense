if (new_addresses_seen <= 6500000) {
    if (src_ipv6_addr <= 5500000) {
        if (port <= 3500000) {
            // Internal Flooding
            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                internal_flooding_tcp_benign_drop();
            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                internal_flooding_tcp_attack_drop();
            } else {
                internal_flooding_ns_drop();
            }
        } else {
            if (protocol <= 500000) {
                if (port <= 9500000) {
                    /* Do nothing, go to next */
                } else {
                    if (src_ipv6_addr <= 2500000) {
                        // Internal Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            internal_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            internal_spoofing_tcp_attack_drop();
                        } else {
                            internal_spoofing_ns_drop();
                        }
                    } else {
                        if (new_addresses_seen <= 5500000) {
                            if (new_addresses_seen <= 3500000) {
                                if (new_addresses_seen <= 1500000) {
                                    if (src_ipv6_addr <= 4500000) {
                                        if (port <= 11500000) {
                                            if (port <= 10500000) {
                                                if (src_ipv6_addr <= 3500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 3500000) {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (port <= 11500000) {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 2500000) {
                                        if (src_ipv6_addr <= 3500000) {
                                            if (port <= 10500000) {
                                                // Internal Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    internal_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    internal_spoofing_tcp_attack_drop();
                                                } else {
                                                    internal_spoofing_ns_drop();
                                                }
                                            } else {
                                                // Internal Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    internal_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    internal_spoofing_tcp_attack_drop();
                                                } else {
                                                    internal_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (port <= 10500000) {
                                                // Internal Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    internal_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    internal_spoofing_tcp_attack_drop();
                                                } else {
                                                    internal_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 4500000) {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 3500000) {
                                            if (port <= 10500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                // Internal Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    internal_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    internal_spoofing_tcp_attack_drop();
                                                } else {
                                                    internal_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (port <= 10500000) {
                                                // Internal Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    internal_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    internal_spoofing_tcp_attack_drop();
                                                } else {
                                                    internal_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (port <= 11500000) {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (new_addresses_seen <= 4500000) {
                                    if (port <= 10500000) {
                                        if (src_ipv6_addr <= 3500000) {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        } else {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 3500000) {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 4500000) {
                                                if (port <= 11500000) {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (port <= 11500000) {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (port <= 10500000) {
                                        if (src_ipv6_addr <= 3500000) {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        } else {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 3500000) {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (port <= 11500000) {
                                                if (src_ipv6_addr <= 4500000) {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 4500000) {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // Internal Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        internal_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        internal_spoofing_tcp_attack_drop();
                                                    } else {
                                                        internal_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (port <= 10500000) {
                                if (src_ipv6_addr <= 3500000) {
                                    /* Do nothing, go to next */
                                } else {
                                    // Internal Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        internal_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        internal_spoofing_tcp_attack_drop();
                                    } else {
                                        internal_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 3500000) {
                                    // Internal Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        internal_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        internal_spoofing_tcp_attack_drop();
                                    } else {
                                        internal_spoofing_ns_drop();
                                    }
                                } else {
                                    if (port <= 11500000) {
                                        if (src_ipv6_addr <= 4500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 4500000) {
                                            // Internal Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                internal_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                internal_spoofing_tcp_attack_drop();
                                            } else {
                                                internal_spoofing_ns_drop();
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            } else {
                /* Do nothing, go to next */
            }
        }
    } else {
        if (new_addresses_seen <= 3500000) {
            if (new_addresses_seen <= 2500000) {
                if (src_ipv6_addr <= 84500000) {
                    if (src_ipv6_addr <= 19500000) {
                        if (hop_count <= 3500000) {
                            if (src_ipv6_addr <= 6500000) {
                                // External Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_spoofing_tcp_attack_drop();
                                } else {
                                    external_spoofing_ns_drop();
                                }
                            } else {
                                if (src_ipv6_addr <= 12500000) {
                                    if (src_ipv6_addr <= 8500000) {
                                        if (src_ipv6_addr <= 7500000) {
                                            if (new_addresses_seen <= 1500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            if (new_addresses_seen <= 1500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        if (new_addresses_seen <= 1500000) {
                                            if (src_ipv6_addr <= 11500000) {
                                                if (src_ipv6_addr <= 10500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 13500000) {
                                        if (new_addresses_seen <= 1500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 14500000) {
                                            if (new_addresses_seen <= 1500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            if (new_addresses_seen <= 1500000) {
                                                if (src_ipv6_addr <= 15500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 15500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (hop_count <= 6500000) {
                                if (src_ipv6_addr <= 12500000) {
                                    if (src_ipv6_addr <= 6500000) {
                                        if (hop_count <= 5500000) {
                                            if (hop_count <= 4500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (new_addresses_seen <= 1500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 5500000) {
                                            if (new_addresses_seen <= 1500000) {
                                                if (src_ipv6_addr <= 9500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 8500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 9000000) {
                                                if (src_ipv6_addr <= 7500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 1500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (hop_count <= 4500000) {
                                        if (src_ipv6_addr <= 14500000) {
                                            if (src_ipv6_addr <= 13500000) {
                                                if (new_addresses_seen <= 1500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 1500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 18500000) {
                                            if (new_addresses_seen <= 1500000) {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 15500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 1500000) {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 5500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        }
                    } else {
                        if (src_ipv6_addr <= 80500000) {
                            if (hop_count <= 5500000) {
                                if (src_ipv6_addr <= 65500000) {
                                    if (src_ipv6_addr <= 59500000) {
                                        if (src_ipv6_addr <= 44500000) {
                                            if (hop_count <= 4500000) {
                                                if (hop_count <= 2500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 27500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                if (src_ipv6_addr <= 45500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 45500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 4500000) {
                                            if (hop_count <= 3500000) {
                                                if (src_ipv6_addr <= 64500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 61500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 64500000) {
                                                if (src_ipv6_addr <= 63500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 1500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (hop_count <= 3500000) {
                                        if (src_ipv6_addr <= 78500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (new_addresses_seen <= 1500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 79500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 4500000) {
                                            if (src_ipv6_addr <= 79500000) {
                                                if (src_ipv6_addr <= 78500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 68500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 69500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 66500000) {
                                    if (hop_count <= 6500000) {
                                        if (new_addresses_seen <= 1500000) {
                                            if (src_ipv6_addr <= 59500000) {
                                                if (src_ipv6_addr <= 27500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 63500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 63500000) {
                                                if (src_ipv6_addr <= 59500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 79500000) {
                                        if (hop_count <= 6500000) {
                                            if (new_addresses_seen <= 1500000) {
                                                if (src_ipv6_addr <= 72000000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 67500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (new_addresses_seen <= 1500000) {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        } else {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (hop_count <= 5500000) {
                                if (hop_count <= 4500000) {
                                    if (src_ipv6_addr <= 83500000) {
                                        if (hop_count <= 3500000) {
                                            if (src_ipv6_addr <= 82500000) {
                                                if (new_addresses_seen <= 1500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 1500000) {
                                                if (src_ipv6_addr <= 82000000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 82500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 81500000) {
                                        if (new_addresses_seen <= 1500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 83500000) {
                                            if (new_addresses_seen <= 1500000) {
                                                if (src_ipv6_addr <= 82500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 82500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                }
                            } else {
                                if (hop_count <= 6500000) {
                                    if (src_ipv6_addr <= 81500000) {
                                        if (new_addresses_seen <= 1500000) {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        } else {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (new_addresses_seen <= 1500000) {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 83500000) {
                                                if (src_ipv6_addr <= 82500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            }
                        }
                    }
                } else {
                    if (hop_count <= 6500000) {
                        if (hop_count <= 5500000) {
                            if (hop_count <= 3500000) {
                                if (src_ipv6_addr <= 102500000) {
                                    if (src_ipv6_addr <= 92500000) {
                                        if (new_addresses_seen <= 1500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 85500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 90000000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 96500000) {
                                            if (new_addresses_seen <= 1500000) {
                                                if (src_ipv6_addr <= 95000000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 95500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 97500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 98500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    // External Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_spoofing_tcp_attack_drop();
                                    } else {
                                        external_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                if (new_addresses_seen <= 1500000) {
                                    if (hop_count <= 4500000) {
                                        if (src_ipv6_addr <= 101500000) {
                                            if (src_ipv6_addr <= 94500000) {
                                                if (src_ipv6_addr <= 93500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 104500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 101000000) {
                                            if (src_ipv6_addr <= 95000000) {
                                                if (src_ipv6_addr <= 87500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 103000000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 104500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (hop_count <= 4500000) {
                                        if (src_ipv6_addr <= 88500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 94000000) {
                                                if (src_ipv6_addr <= 92500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 97500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 90500000) {
                                            if (src_ipv6_addr <= 86000000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 87500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 91500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 104500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 101500000) {
                                if (src_ipv6_addr <= 85500000) {
                                    if (new_addresses_seen <= 1500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                } else {
                                    if (new_addresses_seen <= 1500000) {
                                        if (src_ipv6_addr <= 87500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 97500000) {
                                                if (src_ipv6_addr <= 90500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 98500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 96500000) {
                                            if (src_ipv6_addr <= 91500000) {
                                                if (src_ipv6_addr <= 88500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 92500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 99500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 100500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 104500000) {
                                    if (src_ipv6_addr <= 102500000) {
                                        if (new_addresses_seen <= 1500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 103500000) {
                                            if (new_addresses_seen <= 1500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            if (new_addresses_seen <= 1500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 1500000) {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    } else {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    }
                                }
                            }
                        }
                    } else {
                        if (src_ipv6_addr <= 86000000) {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        } else {
                            if (src_ipv6_addr <= 102000000) {
                                if (src_ipv6_addr <= 91500000) {
                                    if (src_ipv6_addr <= 89000000) {
                                        if (new_addresses_seen <= 1500000) {
                                            if (src_ipv6_addr <= 87500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 96500000) {
                                        if (src_ipv6_addr <= 95500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (new_addresses_seen <= 1500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 104500000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    /* Do nothing, go to next */
                                }
                            }
                        }
                    }
                }
            } else {
                if (hop_count <= 6500000) {
                    if (src_ipv6_addr <= 79500000) {
                        if (src_ipv6_addr <= 22500000) {
                            if (hop_count <= 3500000) {
                                if (src_ipv6_addr <= 19500000) {
                                    if (src_ipv6_addr <= 15500000) {
                                        if (src_ipv6_addr <= 6500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 9000000) {
                                                if (src_ipv6_addr <= 7500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 12500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 18500000) {
                                            if (src_ipv6_addr <= 16500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (src_ipv6_addr <= 17500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    if (hop_count <= 2500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        if (src_ipv6_addr <= 21500000) {
                                            if (src_ipv6_addr <= 20500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 15500000) {
                                    if (src_ipv6_addr <= 12500000) {
                                        if (hop_count <= 5500000) {
                                            if (src_ipv6_addr <= 6500000) {
                                                if (hop_count <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 7500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 9000000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 4500000) {
                                            if (src_ipv6_addr <= 14500000) {
                                                if (src_ipv6_addr <= 13500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 14500000) {
                                                if (src_ipv6_addr <= 13500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 19500000) {
                                        if (src_ipv6_addr <= 18500000) {
                                            if (src_ipv6_addr <= 17500000) {
                                                if (src_ipv6_addr <= 16500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 5500000) {
                                                if (hop_count <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 20500000) {
                                            if (hop_count <= 5500000) {
                                                if (hop_count <= 4500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 21500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 76500000) {
                                if (hop_count <= 2500000) {
                                    /* Do nothing, go to next */
                                } else {
                                    if (src_ipv6_addr <= 24500000) {
                                        if (hop_count <= 3500000) {
                                            if (src_ipv6_addr <= 23500000) {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            } else {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 5500000) {
                                                if (hop_count <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 3500000) {
                                            if (src_ipv6_addr <= 45000000) {
                                                if (src_ipv6_addr <= 41000000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 66000000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 26500000) {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 5500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (hop_count <= 5500000) {
                                    if (src_ipv6_addr <= 78500000) {
                                        if (hop_count <= 3500000) {
                                            if (src_ipv6_addr <= 77500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                if (src_ipv6_addr <= 77500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 77500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 4000000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 77500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        if (src_ipv6_addr <= 78500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                }
                            }
                        }
                    } else {
                        if (hop_count <= 4500000) {
                            if (src_ipv6_addr <= 82500000) {
                                if (src_ipv6_addr <= 80500000) {
                                    if (hop_count <= 3500000) {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (hop_count <= 3500000) {
                                        if (src_ipv6_addr <= 81500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 81500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 99500000) {
                                    if (src_ipv6_addr <= 94500000) {
                                        if (src_ipv6_addr <= 93500000) {
                                            if (src_ipv6_addr <= 90500000) {
                                                if (src_ipv6_addr <= 88500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 3500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 97500000) {
                                                if (src_ipv6_addr <= 96500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 102500000) {
                                        if (hop_count <= 3500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 101500000) {
                                                if (src_ipv6_addr <= 100500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 104500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (hop_count <= 3500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 83500000) {
                                if (hop_count <= 5500000) {
                                    if (src_ipv6_addr <= 81500000) {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 82500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 81500000) {
                                        if (src_ipv6_addr <= 80500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 82500000) {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        } else {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 101500000) {
                                    if (src_ipv6_addr <= 85500000) {
                                        if (src_ipv6_addr <= 84500000) {
                                            if (hop_count <= 5500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 5500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 93500000) {
                                            if (src_ipv6_addr <= 92000000) {
                                                if (src_ipv6_addr <= 88500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 5500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 96500000) {
                                                if (src_ipv6_addr <= 95500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 98500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (hop_count <= 5500000) {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 104500000) {
                                            if (src_ipv6_addr <= 102500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (src_ipv6_addr <= 103500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                } else {
                    if (src_ipv6_addr <= 84500000) {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    } else {
                        if (src_ipv6_addr <= 102000000) {
                            if (src_ipv6_addr <= 93500000) {
                                if (src_ipv6_addr <= 90000000) {
                                    if (src_ipv6_addr <= 87500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                } else {
                                    if (src_ipv6_addr <= 92500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 97500000) {
                                    /* Do nothing, go to next */
                                } else {
                                    if (src_ipv6_addr <= 99500000) {
                                        if (src_ipv6_addr <= 98500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 100500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 104500000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                /* Do nothing, go to next */
                            }
                        }
                    }
                }
            }
        } else {
            if (new_addresses_seen <= 5500000) {
                if (src_ipv6_addr <= 84500000) {
                    if (src_ipv6_addr <= 19500000) {
                        if (hop_count <= 3500000) {
                            if (src_ipv6_addr <= 15500000) {
                                if (src_ipv6_addr <= 12500000) {
                                    if (src_ipv6_addr <= 8500000) {
                                        if (src_ipv6_addr <= 7500000) {
                                            if (src_ipv6_addr <= 6500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 4500000) {
                                        if (src_ipv6_addr <= 14500000) {
                                            if (src_ipv6_addr <= 13500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 13500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (src_ipv6_addr <= 14500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 18500000) {
                                    if (src_ipv6_addr <= 16500000) {
                                        if (new_addresses_seen <= 4500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                } else {
                                    if (new_addresses_seen <= 4500000) {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                }
                            }
                        } else {
                            if (hop_count <= 6500000) {
                                if (src_ipv6_addr <= 15500000) {
                                    if (src_ipv6_addr <= 10500000) {
                                        if (hop_count <= 4500000) {
                                            if (src_ipv6_addr <= 8500000) {
                                                if (src_ipv6_addr <= 7500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 5500000) {
                                                if (src_ipv6_addr <= 7500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 4500000) {
                                            if (src_ipv6_addr <= 14500000) {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 13500000) {
                                                if (src_ipv6_addr <= 12500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 4500000) {
                                        if (hop_count <= 4500000) {
                                            if (src_ipv6_addr <= 17000000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 18500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 5500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 17500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 18500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        }
                    } else {
                        if (src_ipv6_addr <= 79500000) {
                            if (hop_count <= 5500000) {
                                if (src_ipv6_addr <= 64500000) {
                                    if (src_ipv6_addr <= 59500000) {
                                        if (src_ipv6_addr <= 44500000) {
                                            if (hop_count <= 4500000) {
                                                if (src_ipv6_addr <= 39500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 42500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 55500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 4500000) {
                                            if (new_addresses_seen <= 4500000) {
                                                if (src_ipv6_addr <= 63500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 3500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 62500000) {
                                                if (src_ipv6_addr <= 60500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 63500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (hop_count <= 3500000) {
                                        if (src_ipv6_addr <= 71500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 73500000) {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 78500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 78500000) {
                                            if (src_ipv6_addr <= 77500000) {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 72500000) {
                                    if (hop_count <= 6500000) {
                                        if (new_addresses_seen <= 4500000) {
                                            if (src_ipv6_addr <= 58500000) {
                                                if (src_ipv6_addr <= 25500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 69500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 20500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 27500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 78500000) {
                                        if (src_ipv6_addr <= 75500000) {
                                            if (new_addresses_seen <= 4500000) {
                                                if (src_ipv6_addr <= 74500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (hop_count <= 6500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 4500000) {
                                                if (src_ipv6_addr <= 77500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 77500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 6500000) {
                                            if (new_addresses_seen <= 4500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (hop_count <= 5500000) {
                                if (new_addresses_seen <= 4500000) {
                                    if (hop_count <= 3500000) {
                                        if (src_ipv6_addr <= 82500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 83500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 83500000) {
                                            if (src_ipv6_addr <= 82500000) {
                                                if (src_ipv6_addr <= 80500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 81500000) {
                                        if (hop_count <= 4500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 80500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 83500000) {
                                            if (hop_count <= 4500000) {
                                                if (hop_count <= 3500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 82500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (hop_count <= 6500000) {
                                    if (new_addresses_seen <= 4500000) {
                                        if (src_ipv6_addr <= 81500000) {
                                            if (src_ipv6_addr <= 80500000) {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            } else {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 82500000) {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 83500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 81500000) {
                                            if (src_ipv6_addr <= 80500000) {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            } else {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 82500000) {
                                                // External Flooding
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_tcp_attack_drop();
                                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                    external_flooding_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 83500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            }
                        }
                    }
                } else {
                    if (hop_count <= 6500000) {
                        if (src_ipv6_addr <= 101500000) {
                            if (hop_count <= 3500000) {
                                if (src_ipv6_addr <= 91500000) {
                                    if (src_ipv6_addr <= 90500000) {
                                        if (src_ipv6_addr <= 88500000) {
                                            if (new_addresses_seen <= 4500000) {
                                                if (src_ipv6_addr <= 86500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 89500000) {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (new_addresses_seen <= 4500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 100500000) {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (new_addresses_seen <= 4500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (new_addresses_seen <= 4500000) {
                                    if (src_ipv6_addr <= 99500000) {
                                        if (src_ipv6_addr <= 92500000) {
                                            if (src_ipv6_addr <= 90500000) {
                                                if (src_ipv6_addr <= 86500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 91500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 93500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 94500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 100500000) {
                                            if (hop_count <= 4500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 5000000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 96500000) {
                                        if (src_ipv6_addr <= 89500000) {
                                            if (hop_count <= 5500000) {
                                                if (src_ipv6_addr <= 87500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 85500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 95500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 97500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 98500000) {
                                                if (hop_count <= 4500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 99500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (hop_count <= 5500000) {
                                if (new_addresses_seen <= 4500000) {
                                    if (src_ipv6_addr <= 102500000) {
                                        if (hop_count <= 4500000) {
                                            if (hop_count <= 3500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 4500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 103500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (hop_count <= 4500000) {
                                        if (src_ipv6_addr <= 103500000) {
                                            if (hop_count <= 3500000) {
                                                if (src_ipv6_addr <= 102500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 102500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 104500000) {
                                            if (src_ipv6_addr <= 103500000) {
                                                if (src_ipv6_addr <= 102500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 104500000) {
                                    if (new_addresses_seen <= 4500000) {
                                        if (src_ipv6_addr <= 102500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 103500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 102500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 103500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 4500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    }
                                }
                            }
                        }
                    } else {
                        if (src_ipv6_addr <= 93500000) {
                            if (new_addresses_seen <= 4500000) {
                                if (src_ipv6_addr <= 90000000) {
                                    /* Do nothing, go to next */
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 101500000) {
                                if (src_ipv6_addr <= 95500000) {
                                    if (src_ipv6_addr <= 94500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 98500000) {
                                        if (new_addresses_seen <= 4500000) {
                                            if (src_ipv6_addr <= 97500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 99500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                }
                            } else {
                                if (new_addresses_seen <= 4500000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    /* Do nothing, go to next */
                                }
                            }
                        }
                    }
                }
            } else {
                if (hop_count <= 6500000) {
                    if (src_ipv6_addr <= 84500000) {
                        if (src_ipv6_addr <= 24500000) {
                            if (src_ipv6_addr <= 12000000) {
                                if (src_ipv6_addr <= 6500000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    if (hop_count <= 3500000) {
                                        if (src_ipv6_addr <= 8500000) {
                                            if (src_ipv6_addr <= 7500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 10500000) {
                                            if (hop_count <= 4500000) {
                                                if (src_ipv6_addr <= 8500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 7500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 19500000) {
                                    if (hop_count <= 3500000) {
                                        if (src_ipv6_addr <= 15500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (src_ipv6_addr <= 17500000) {
                                                if (src_ipv6_addr <= 16500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (hop_count <= 2500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        if (hop_count <= 3500000) {
                                            if (src_ipv6_addr <= 21500000) {
                                                if (src_ipv6_addr <= 20500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 23500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (hop_count <= 3500000) {
                                if (src_ipv6_addr <= 26500000) {
                                    if (src_ipv6_addr <= 25500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 44500000) {
                                        if (src_ipv6_addr <= 41500000) {
                                            if (src_ipv6_addr <= 30500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 31500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 42500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 43500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 58500000) {
                                            if (src_ipv6_addr <= 56000000) {
                                                if (src_ipv6_addr <= 54500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 75500000) {
                                                if (src_ipv6_addr <= 70500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 78000000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 33500000) {
                                    if (hop_count <= 4500000) {
                                        if (src_ipv6_addr <= 32500000) {
                                            if (src_ipv6_addr <= 28500000) {
                                                if (src_ipv6_addr <= 25500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 25500000) {
                                            if (hop_count <= 5500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 28500000) {
                                                if (src_ipv6_addr <= 26500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 29500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 38500000) {
                                        if (src_ipv6_addr <= 34500000) {
                                            if (hop_count <= 4500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                if (src_ipv6_addr <= 37500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 44500000) {
                                            if (hop_count <= 4500000) {
                                                if (src_ipv6_addr <= 39500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 41500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                if (src_ipv6_addr <= 64500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 54500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    } else {
                        if (src_ipv6_addr <= 86500000) {
                            if (hop_count <= 5500000) {
                                if (src_ipv6_addr <= 85500000) {
                                    if (hop_count <= 3500000) {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (hop_count <= 4500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 85500000) {
                                    // External Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_spoofing_tcp_attack_drop();
                                    } else {
                                        external_spoofing_ns_drop();
                                    }
                                } else {
                                    // External Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_spoofing_tcp_attack_drop();
                                    } else {
                                        external_spoofing_ns_drop();
                                    }
                                }
                            }
                        } else {
                            if (hop_count <= 3500000) {
                                if (src_ipv6_addr <= 89500000) {
                                    if (src_ipv6_addr <= 88500000) {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    } else {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    // External Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_spoofing_tcp_attack_drop();
                                    } else {
                                        external_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 95500000) {
                                    if (hop_count <= 4500000) {
                                        if (src_ipv6_addr <= 92500000) {
                                            if (src_ipv6_addr <= 89500000) {
                                                if (src_ipv6_addr <= 88500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 93500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 91500000) {
                                            if (src_ipv6_addr <= 87500000) {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 89500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 94500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 98000000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 100500000) {
                                            if (hop_count <= 5500000) {
                                                if (src_ipv6_addr <= 99500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 103500000) {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 104500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                } else {
                    if (src_ipv6_addr <= 85000000) {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    } else {
                        if (src_ipv6_addr <= 94500000) {
                            if (src_ipv6_addr <= 88500000) {
                                /* Do nothing, go to next */
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        } else {
                            /* Do nothing, go to next */
                        }
                    }
                }
            }
        }
    }
} else {
    if (new_addresses_seen <= 7500000) {
        if (src_ipv6_addr <= 84500000) {
            if (src_ipv6_addr <= 79500000) {
                if (hop_count <= 4500000) {
                    if (src_ipv6_addr <= 45000000) {
                        if (src_ipv6_addr <= 39500000) {
                            if (hop_count <= 3500000) {
                                if (src_ipv6_addr <= 20500000) {
                                    if (src_ipv6_addr <= 15500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                } else {
                                    if (src_ipv6_addr <= 23500000) {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 38500000) {
                                    if (src_ipv6_addr <= 33500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 35500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                } else {
                                    /* Do nothing, go to next */
                                }
                            }
                        } else {
                            if (hop_count <= 3500000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                if (src_ipv6_addr <= 42500000) {
                                    if (src_ipv6_addr <= 40500000) {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    } else {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 43500000) {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    } else {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    }
                                }
                            }
                        }
                    } else {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    }
                } else {
                    if (src_ipv6_addr <= 59500000) {
                        if (src_ipv6_addr <= 54000000) {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        } else {
                            if (hop_count <= 5500000) {
                                if (src_ipv6_addr <= 57500000) {
                                    if (src_ipv6_addr <= 55500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    /* Do nothing, go to next */
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        }
                    } else {
                        if (src_ipv6_addr <= 65000000) {
                            if (hop_count <= 5500000) {
                                if (src_ipv6_addr <= 61500000) {
                                    // External Flooding
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_tcp_attack_drop();
                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                        external_flooding_ns_drop();
                                    }
                                } else {
                                    if (src_ipv6_addr <= 63500000) {
                                        if (src_ipv6_addr <= 62500000) {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        } else {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        }
                                    } else {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    }
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        } else {
                            if (hop_count <= 5500000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                if (src_ipv6_addr <= 72500000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    if (src_ipv6_addr <= 73500000) {
                                        if (hop_count <= 6500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 75500000) {
                                            if (src_ipv6_addr <= 74500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            } else {
                if (hop_count <= 5500000) {
                    // External Flooding Spoofing
                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                        external_flooding_spoofing_tcp_benign_drop();
                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                        external_flooding_spoofing_tcp_attack_drop();
                    } else {
                        external_flooding_spoofing_ns_drop();
                    }
                } else {
                    if (hop_count <= 6500000) {
                        if (src_ipv6_addr <= 80500000) {
                            // External Flooding
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_tcp_attack_drop();
                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                external_flooding_ns_drop();
                            }
                        } else {
                            if (src_ipv6_addr <= 83500000) {
                                if (src_ipv6_addr <= 82500000) {
                                    if (src_ipv6_addr <= 81500000) {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    } else {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    }
                                } else {
                                    // External Flooding
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_tcp_attack_drop();
                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                        external_flooding_ns_drop();
                                    }
                                }
                            } else {
                                // External Flooding
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_tcp_attack_drop();
                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                    external_flooding_ns_drop();
                                }
                            }
                        }
                    } else {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    }
                }
            }
        } else {
            // External Flooding Spoofing
            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                external_flooding_spoofing_tcp_benign_drop();
            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                external_flooding_spoofing_tcp_attack_drop();
            } else {
                external_flooding_spoofing_ns_drop();
            }
        }
    } else {
        if (new_addresses_seen <= 8500000) {
            if (src_ipv6_addr <= 79500000) {
                if (src_ipv6_addr <= 74500000) {
                    if (hop_count <= 5500000) {
                        if (src_ipv6_addr <= 19000000) {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        } else {
                            if (src_ipv6_addr <= 20500000) {
                                if (hop_count <= 3500000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 38500000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    if (src_ipv6_addr <= 39500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (hop_count <= 4500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 59500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 62000000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    } else {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    }
                } else {
                    if (src_ipv6_addr <= 76000000) {
                        if (hop_count <= 5000000) {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        } else {
                            /* Do nothing, go to next */
                        }
                    } else {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    }
                }
            } else {
                if (src_ipv6_addr <= 84500000) {
                    if (hop_count <= 5500000) {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    } else {
                        if (hop_count <= 6500000) {
                            if (src_ipv6_addr <= 80500000) {
                                // External Flooding
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_tcp_attack_drop();
                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                    external_flooding_ns_drop();
                                }
                            } else {
                                if (src_ipv6_addr <= 82500000) {
                                    if (src_ipv6_addr <= 81500000) {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    } else {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    }
                                } else {
                                    // External Flooding
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_tcp_attack_drop();
                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                        external_flooding_ns_drop();
                                    }
                                }
                            }
                        } else {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        }
                    }
                } else {
                    // External Flooding Spoofing
                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                        external_flooding_spoofing_tcp_benign_drop();
                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                        external_flooding_spoofing_tcp_attack_drop();
                    } else {
                        external_flooding_spoofing_ns_drop();
                    }
                }
            }
        } else {
            if (src_ipv6_addr <= 98500000) {
                if (hop_count <= 3500000) {
                    if (src_ipv6_addr <= 21500000) {
                        if (src_ipv6_addr <= 18500000) {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        } else {
                            if (new_addresses_seen <= 19500000) {
                                if (new_addresses_seen <= 16500000) {
                                    if (new_addresses_seen <= 15500000) {
                                        if (src_ipv6_addr <= 19500000) {
                                            if (new_addresses_seen <= 11000000) {
                                                if (new_addresses_seen <= 9500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            if (new_addresses_seen <= 12500000) {
                                                if (src_ipv6_addr <= 20500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 20500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 19500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 17500000) {
                                        if (src_ipv6_addr <= 19500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 19500000) {
                                            if (new_addresses_seen <= 18500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 20500000) {
                                                if (new_addresses_seen <= 18500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (new_addresses_seen <= 18500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 20500000) {
                                    if (src_ipv6_addr <= 19500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    /* Do nothing, go to next */
                                }
                            }
                        }
                    } else {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    }
                } else {
                    if (src_ipv6_addr <= 38500000) {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    } else {
                        if (src_ipv6_addr <= 41500000) {
                            if (hop_count <= 4500000) {
                                if (new_addresses_seen <= 15500000) {
                                    if (new_addresses_seen <= 14500000) {
                                        if (src_ipv6_addr <= 39500000) {
                                            if (new_addresses_seen <= 12500000) {
                                                if (new_addresses_seen <= 11000000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 13500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 9500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (new_addresses_seen <= 10500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 39500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 18500000) {
                                        if (src_ipv6_addr <= 40500000) {
                                            if (new_addresses_seen <= 17500000) {
                                                if (src_ipv6_addr <= 39500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 17500000) {
                                                if (new_addresses_seen <= 16500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (new_addresses_seen <= 19500000) {
                                            if (src_ipv6_addr <= 39500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (src_ipv6_addr <= 40500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 39500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 40500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 58500000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                if (src_ipv6_addr <= 61500000) {
                                    if (hop_count <= 5500000) {
                                        if (hop_count <= 4500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (new_addresses_seen <= 19500000) {
                                                if (new_addresses_seen <= 15500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 60500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 12500000) {
                                        if (hop_count <= 5500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 78500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 81500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 5500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 78500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 81500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            } else {
                if (hop_count <= 6500000) {
                    // External Flooding Spoofing
                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                        external_flooding_spoofing_tcp_benign_drop();
                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                        external_flooding_spoofing_tcp_attack_drop();
                    } else {
                        external_flooding_spoofing_ns_drop();
                    }
                } else {
                    if (src_ipv6_addr <= 101500000) {
                        if (new_addresses_seen <= 10500000) {
                            if (src_ipv6_addr <= 99500000) {
                                if (new_addresses_seen <= 9500000) {
                                    /* Do nothing, go to next */
                                } else {
                                    /* Do nothing, go to next */
                                }
                            } else {
                                /* Do nothing, go to next */
                            }
                        } else {
                            if (new_addresses_seen <= 16500000) {
                                if (src_ipv6_addr <= 99500000) {
                                    /* Do nothing, go to next */
                                } else {
                                    if (src_ipv6_addr <= 100500000) {
                                        if (new_addresses_seen <= 14500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (new_addresses_seen <= 15500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (new_addresses_seen <= 15500000) {
                                            if (new_addresses_seen <= 11500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (new_addresses_seen <= 12500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (new_addresses_seen <= 18500000) {
                                    if (new_addresses_seen <= 17500000) {
                                        if (src_ipv6_addr <= 99500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (src_ipv6_addr <= 100500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 100500000) {
                                            if (src_ipv6_addr <= 99500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 19500000) {
                                        if (src_ipv6_addr <= 99500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (src_ipv6_addr <= 100500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 99500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (src_ipv6_addr <= 100500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    } else {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    }
                }
            }
        }
    }
}