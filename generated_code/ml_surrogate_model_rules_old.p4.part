if (inter_packet_time <= 190000) {
    if (new_addresses_seen <= 500000) {
        if (port <= 5000000) {
            // Internal Flooding
            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                internal_flooding_tcp_benign_drop();
            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                internal_flooding_tcp_attack_drop();
            } else {
                internal_flooding_ns_drop();
            }
        } else {
            // Internal Spoofing
            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                internal_spoofing_tcp_benign_drop();
            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                internal_spoofing_tcp_attack_drop();
            } else {
                internal_spoofing_ns_drop();
            }
        }
    } else {
        if (new_addresses_seen <= 7500000) {
            if (hop_count <= 2500000) {
                /* Do nothing, go to next */
            } else {
                if (inter_packet_time <= 10000) {
                    if (src_ipv6_addr <= 84500000) {
                        if (src_ipv6_addr <= 19500000) {
                            if (new_addresses_seen <= 6500000) {
                                if (inter_packet_time <= -3820000) {
                                    if (src_ipv6_addr <= 7500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                } else {
                                    if (hop_count <= 6500000) {
                                        if (inter_packet_time <= 10000) {
                                            if (hop_count <= 3500000) {
                                                if (src_ipv6_addr <= 15500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 0) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (inter_packet_time <= 10000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (new_addresses_seen <= 1500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        } else {
                            if (new_addresses_seen <= 5500000) {
                                if (src_ipv6_addr <= 79500000) {
                                    if (hop_count <= 5500000) {
                                        if (src_ipv6_addr <= 64500000) {
                                            if (src_ipv6_addr <= 59500000) {
                                                if (hop_count <= 4500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 4500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (inter_packet_time <= -15530000) {
                                                if (prefix_inter_packet_time <= -104160000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 0) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 6500000) {
                                            if (src_ipv6_addr <= 75500000) {
                                                if (new_addresses_seen <= 2500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 0) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                } else {
                                    if (hop_count <= 5500000) {
                                        if (prefix_inter_packet_time <= -15470000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (new_addresses_seen <= 2500000) {
                                                if (prefix_inter_packet_time <= 0) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 10000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 6500000) {
                                            if (inter_packet_time <= -23550000) {
                                                if (prefix_inter_packet_time <= -83240000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 81500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (new_addresses_seen <= 6500000) {
                                    if (src_ipv6_addr <= 24500000) {
                                        if (hop_count <= 3500000) {
                                            if (inter_packet_time <= -2120000) {
                                                if (src_ipv6_addr <= 22000000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (inter_packet_time <= 10000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 3500000) {
                                            if (prefix_inter_packet_time <= -15450000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (inter_packet_time <= 0) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 39500000) {
                                                if (inter_packet_time <= -15580000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 44500000) {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (inter_packet_time <= 10000) {
                                        if (hop_count <= 6500000) {
                                            if (prefix_inter_packet_time <= -82320000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 20500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 47000000) {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 78000000) {
                                                if (hop_count <= 5500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 4500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    } else {
                        if (hop_count <= 6500000) {
                            if (new_addresses_seen <= 6500000) {
                                if (prefix_inter_packet_time <= -15500000) {
                                    if (new_addresses_seen <= 4500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (prefix_inter_packet_time <= 10000) {
                                        if (inter_packet_time <= 10000) {
                                            if (inter_packet_time <= 0) {
                                                if (inter_packet_time <= 0) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 2500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 2500000) {
                                                if (inter_packet_time <= 10000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 10000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 94500000) {
                                if (prefix_inter_packet_time <= -15490000) {
                                    /* Do nothing, go to next */
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 98500000) {
                                    if (inter_packet_time <= 10000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        if (new_addresses_seen <= 2500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    if (inter_packet_time <= -15480000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                }
                            }
                        }
                    }
                } else {
                    if (new_addresses_seen <= 6500000) {
                        if (hop_count <= 6500000) {
                            if (new_addresses_seen <= 3500000) {
                                if (new_addresses_seen <= 2500000) {
                                    if (hop_count <= 3500000) {
                                        if (inter_packet_time <= 10000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (prefix_inter_packet_time <= 100000) {
                                                if (inter_packet_time <= 90000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 47000000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (new_addresses_seen <= 1500000) {
                                            if (src_ipv6_addr <= 24500000) {
                                                if (src_ipv6_addr <= 13500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 31500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (inter_packet_time <= 20000) {
                                                if (src_ipv6_addr <= 60500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (inter_packet_time <= 20000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 15500000) {
                                        if (hop_count <= 3500000) {
                                            if (src_ipv6_addr <= 12500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 40000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            if (prefix_inter_packet_time <= 20000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 10500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 3500000) {
                                            if (src_ipv6_addr <= 28500000) {
                                                if (src_ipv6_addr <= 19500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 180000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            if (prefix_inter_packet_time <= 130000) {
                                                if (src_ipv6_addr <= 18500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 39500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (prefix_inter_packet_time <= 20000) {
                                    if (src_ipv6_addr <= 84500000) {
                                        if (new_addresses_seen <= 5500000) {
                                            if (src_ipv6_addr <= 79500000) {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_tcp_attack_drop();
                                                    } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                        external_flooding_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 25500000) {
                                                if (src_ipv6_addr <= 14500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (inter_packet_time <= 20000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (prefix_inter_packet_time <= 10000) {
                                            if (inter_packet_time <= 10000) {
                                                if (src_ipv6_addr <= 91500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 4500000) {
                                                if (inter_packet_time <= 20000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (hop_count <= 3500000) {
                                        if (src_ipv6_addr <= 26500000) {
                                            if (src_ipv6_addr <= 14500000) {
                                                if (prefix_inter_packet_time <= 20000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 5500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 4500000) {
                                                if (src_ipv6_addr <= 101500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 190000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (new_addresses_seen <= 4500000) {
                                            if (src_ipv6_addr <= 100500000) {
                                                if (src_ipv6_addr <= 25500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 50000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 6500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 25500000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 92000000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                if (src_ipv6_addr <= 101500000) {
                                    if (inter_packet_time <= 60000) {
                                        if (src_ipv6_addr <= 100500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (prefix_inter_packet_time <= 30000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        if (inter_packet_time <= 100000) {
                                            if (inter_packet_time <= 100000) {
                                                if (prefix_inter_packet_time <= 70000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 97000000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (new_addresses_seen <= 4500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            }
                        }
                    } else {
                        if (hop_count <= 3500000) {
                            if (src_ipv6_addr <= 23500000) {
                                if (src_ipv6_addr <= 15500000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    /* Do nothing, go to next */
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        } else {
                            if (prefix_inter_packet_time <= 180000) {
                                if (prefix_inter_packet_time <= 10000) {
                                    if (inter_packet_time <= 10000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    }
                                } else {
                                    if (prefix_inter_packet_time <= 20000) {
                                        if (prefix_inter_packet_time <= 10000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Flooding
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_tcp_attack_drop();
                                            } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                                external_flooding_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (inter_packet_time <= 40000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (inter_packet_time <= 40000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (src_ipv6_addr <= 56000000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                /* Do nothing, go to next */
                            }
                        }
                    }
                }
            }
        } else {
            if (new_addresses_seen <= 8500000) {
                if (prefix_inter_packet_time <= 10000) {
                    if (src_ipv6_addr <= 79500000) {
                        if (prefix_inter_packet_time <= 10000) {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        } else {
                            /* Do nothing, go to next */
                        }
                    } else {
                        if (src_ipv6_addr <= 86000000) {
                            if (prefix_inter_packet_time <= -22840000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                // External Flooding
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_tcp_attack_drop();
                                } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                    external_flooding_ns_drop();
                                }
                            }
                        } else {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        }
                    }
                } else {
                    if (prefix_inter_packet_time <= 20000) {
                        if (inter_packet_time <= 20000) {
                            if (hop_count <= 5500000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                if (hop_count <= 6500000) {
                                    if (inter_packet_time <= 10000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Flooding
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_tcp_attack_drop();
                                        } else if (hdr.icmpv6.isValid() && hdr.icmpv6.type == TYPE_ICMPV6_NS) {
                                            external_flooding_ns_drop();
                                        }
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            }
                        } else {
                            /* Do nothing, go to next */
                        }
                    } else {
                        if (prefix_inter_packet_time <= 70000) {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        } else {
                            if (prefix_inter_packet_time <= 80000) {
                                /* Do nothing, go to next */
                            } else {
                                if (inter_packet_time <= 90000) {
                                    if (inter_packet_time <= 90000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            }
                        }
                    }
                }
            } else {
                if (src_ipv6_addr <= 98500000) {
                    if (hop_count <= 3500000) {
                        if (src_ipv6_addr <= 21500000) {
                            if (src_ipv6_addr <= 18500000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                if (prefix_inter_packet_time <= 50000) {
                                    if (inter_packet_time <= 20000) {
                                        if (prefix_inter_packet_time <= 20000) {
                                            if (prefix_inter_packet_time <= 20000) {
                                                if (prefix_inter_packet_time <= 10000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 20500000) {
                                            if (new_addresses_seen <= 19500000) {
                                                if (inter_packet_time <= 30000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 20000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 18500000) {
                                                if (inter_packet_time <= 20000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 40000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (inter_packet_time <= 170000) {
                                        if (inter_packet_time <= 150000) {
                                            if (inter_packet_time <= 120000) {
                                                if (new_addresses_seen <= 16500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (new_addresses_seen <= 17000000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                }
                            }
                        } else {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        }
                    } else {
                        if (src_ipv6_addr <= 38500000) {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        } else {
                            if (src_ipv6_addr <= 41500000) {
                                if (hop_count <= 4500000) {
                                    if (inter_packet_time <= 30000) {
                                        if (inter_packet_time <= 20000) {
                                            if (inter_packet_time <= 10000) {
                                                if (inter_packet_time <= 0) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (inter_packet_time <= 10000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (inter_packet_time <= 20000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (new_addresses_seen <= 16000000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (prefix_inter_packet_time <= 180000) {
                                            if (new_addresses_seen <= 9500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (new_addresses_seen <= 13500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                if (hop_count <= 4500000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    if (src_ipv6_addr <= 58500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 61500000) {
                                            if (hop_count <= 5500000) {
                                                if (new_addresses_seen <= 19500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 78500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 81500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                } else {
                    if (hop_count <= 6500000) {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    } else {
                        if (src_ipv6_addr <= 101500000) {
                            if (new_addresses_seen <= 10500000) {
                                if (src_ipv6_addr <= 99500000) {
                                    if (prefix_inter_packet_time <= 30000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        if (prefix_inter_packet_time <= 60000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    /* Do nothing, go to next */
                                }
                            } else {
                                if (new_addresses_seen <= 16500000) {
                                    if (src_ipv6_addr <= 99500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        if (prefix_inter_packet_time <= 170000) {
                                            if (inter_packet_time <= 90000) {
                                                if (prefix_inter_packet_time <= 70000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    if (prefix_inter_packet_time <= 30000) {
                                        if (inter_packet_time <= 10000) {
                                            if (inter_packet_time <= 0) {
                                                if (src_ipv6_addr <= 100000000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (new_addresses_seen <= 19500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (src_ipv6_addr <= 100500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        if (prefix_inter_packet_time <= 50000) {
                                            if (inter_packet_time <= 40000) {
                                                if (prefix_inter_packet_time <= 30000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 99500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (inter_packet_time <= 70000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (inter_packet_time <= 70000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        }
                    }
                }
            }
        }
    }
} else {
    if (new_addresses_seen <= 500000) {
        if (port <= 5000000) {
            // Internal Flooding
            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                internal_flooding_tcp_benign_drop();
            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                internal_flooding_tcp_attack_drop();
            } else {
                internal_flooding_ns_drop();
            }
        } else {
            // Internal Spoofing
            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                internal_spoofing_tcp_benign_drop();
            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                internal_spoofing_tcp_attack_drop();
            } else {
                internal_spoofing_ns_drop();
            }
        }
    } else {
        if (new_addresses_seen <= 6500000) {
            if (inter_packet_time <= 2160000) {
                if (prefix_inter_packet_time <= 210000) {
                    if (src_ipv6_addr <= 16500000) {
                        if (hop_count <= 3500000) {
                            /* Do nothing, go to next */
                        } else {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        }
                    } else {
                        if (new_addresses_seen <= 3500000) {
                            if (new_addresses_seen <= 2500000) {
                                if (hop_count <= 3500000) {
                                    // External Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_spoofing_tcp_attack_drop();
                                    } else {
                                        external_spoofing_ns_drop();
                                    }
                                } else {
                                    if (inter_packet_time <= 200000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 70500000) {
                                            if (src_ipv6_addr <= 61500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                }
                            } else {
                                if (hop_count <= 3500000) {
                                    if (src_ipv6_addr <= 31000000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        if (src_ipv6_addr <= 40500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    if (prefix_inter_packet_time <= 200000) {
                                        if (inter_packet_time <= 190000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 92500000) {
                                            if (hop_count <= 6500000) {
                                                if (src_ipv6_addr <= 86500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                }
                            }
                        } else {
                            if (hop_count <= 3500000) {
                                if (src_ipv6_addr <= 20500000) {
                                    if (new_addresses_seen <= 5000000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 101500000) {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                }
                            } else {
                                if (new_addresses_seen <= 4500000) {
                                    if (src_ipv6_addr <= 77000000) {
                                        if (src_ipv6_addr <= 51500000) {
                                            if (hop_count <= 6000000) {
                                                if (prefix_inter_packet_time <= 210000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (hop_count <= 5500000) {
                                        if (src_ipv6_addr <= 62500000) {
                                            if (src_ipv6_addr <= 52000000) {
                                                if (inter_packet_time <= 200000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 6500000) {
                                            if (src_ipv6_addr <= 102000000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                }
                            }
                        }
                    }
                } else {
                    if (new_addresses_seen <= 3500000) {
                        if (new_addresses_seen <= 2500000) {
                            if (inter_packet_time <= 240000) {
                                if (prefix_inter_packet_time <= 210000) {
                                    if (prefix_inter_packet_time <= 210000) {
                                        if (hop_count <= 3500000) {
                                            if (src_ipv6_addr <= 32000000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (prefix_inter_packet_time <= 230000) {
                                        if (inter_packet_time <= 220000) {
                                            if (inter_packet_time <= 220000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (prefix_inter_packet_time <= 220000) {
                                                if (inter_packet_time <= 220000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 97500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    } else {
                                        if (prefix_inter_packet_time <= 230000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (inter_packet_time <= 230000) {
                                                if (inter_packet_time <= 230000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 5500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (hop_count <= 3500000) {
                                    if (src_ipv6_addr <= 70000000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 27000000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (inter_packet_time <= 290000) {
                                            if (prefix_inter_packet_time <= 240000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 31000000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            if (prefix_inter_packet_time <= 300000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 47500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (prefix_inter_packet_time <= 290000) {
                                if (inter_packet_time <= 210000) {
                                    if (inter_packet_time <= 210000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_spoofing_tcp_attack_drop();
                                        } else {
                                            external_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 11500000) {
                                        if (src_ipv6_addr <= 9000000) {
                                            if (prefix_inter_packet_time <= 220000) {
                                                if (prefix_inter_packet_time <= 220000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        if (inter_packet_time <= 220000) {
                                            if (prefix_inter_packet_time <= 220000) {
                                                if (src_ipv6_addr <= 14500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (inter_packet_time <= 250000) {
                                                if (inter_packet_time <= 230000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (inter_packet_time <= 250000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (prefix_inter_packet_time <= 290000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    if (hop_count <= 4500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        if (src_ipv6_addr <= 40500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (inter_packet_time <= 380000) {
                                                if (inter_packet_time <= 300000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (inter_packet_time <= 380000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    } else {
                        if (hop_count <= 2500000) {
                            /* Do nothing, go to next */
                        } else {
                            if (src_ipv6_addr <= 12000000) {
                                if (inter_packet_time <= 230000) {
                                    if (hop_count <= 3500000) {
                                        if (src_ipv6_addr <= 8500000) {
                                            if (inter_packet_time <= 210000) {
                                                // External Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (inter_packet_time <= 220000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (inter_packet_time <= 290000) {
                                        if (new_addresses_seen <= 5500000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 7500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (prefix_inter_packet_time <= 240000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    } else {
                                        if (hop_count <= 5000000) {
                                            // External Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_spoofing_tcp_attack_drop();
                                            } else {
                                                external_spoofing_ns_drop();
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            } else {
                                if (inter_packet_time <= 270000) {
                                    if (prefix_inter_packet_time <= 220000) {
                                        if (src_ipv6_addr <= 81000000) {
                                            if (hop_count <= 6500000) {
                                                if (src_ipv6_addr <= 27000000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 6500000) {
                                                if (src_ipv6_addr <= 101000000) {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 78500000) {
                                            if (inter_packet_time <= 240000) {
                                                if (hop_count <= 5500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (prefix_inter_packet_time <= 240000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            if (hop_count <= 4500000) {
                                                if (inter_packet_time <= 220000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (hop_count <= 6500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 4500000) {
                                        if (hop_count <= 4000000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (hop_count <= 6500000) {
                                                if (inter_packet_time <= 290000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        if (prefix_inter_packet_time <= 540000) {
                                            if (prefix_inter_packet_time <= 300000) {
                                                if (new_addresses_seen <= 5500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 48500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            } else {
                if (hop_count <= 3500000) {
                    /* Do nothing, go to next */
                } else {
                    if (inter_packet_time <= 2600000) {
                        if (inter_packet_time <= 2480000) {
                            // External Flooding Spoofing
                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                external_flooding_spoofing_tcp_benign_drop();
                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                external_flooding_spoofing_tcp_attack_drop();
                            } else {
                                external_flooding_spoofing_ns_drop();
                            }
                        } else {
                            /* Do nothing, go to next */
                        }
                    } else {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    }
                }
            }
        } else {
            if (prefix_inter_packet_time <= 240000) {
                if (prefix_inter_packet_time <= 220000) {
                    if (new_addresses_seen <= 15500000) {
                        if (inter_packet_time <= 210000) {
                            if (inter_packet_time <= 200000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                if (prefix_inter_packet_time <= 200000) {
                                    if (inter_packet_time <= 200000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        if (prefix_inter_packet_time <= 200000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    }
                                } else {
                                    if (new_addresses_seen <= 9500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (prefix_inter_packet_time <= 210000) {
                                            if (src_ipv6_addr <= 59500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 82500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        } else {
                                            if (inter_packet_time <= 210000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (new_addresses_seen <= 12500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 52500000) {
                                if (new_addresses_seen <= 7500000) {
                                    /* Do nothing, go to next */
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                if (prefix_inter_packet_time <= 220000) {
                                    if (inter_packet_time <= 210000) {
                                        if (hop_count <= 6000000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 98000000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            }
                        }
                    } else {
                        if (prefix_inter_packet_time <= 210000) {
                            if (prefix_inter_packet_time <= 190000) {
                                if (inter_packet_time <= 190000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    /* Do nothing, go to next */
                                }
                            } else {
                                if (hop_count <= 4500000) {
                                    if (src_ipv6_addr <= 41500000) {
                                        if (src_ipv6_addr <= 39500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (hop_count <= 3500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                /* Do nothing, go to next */
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            }
                        } else {
                            if (prefix_inter_packet_time <= 210000) {
                                /* Do nothing, go to next */
                            } else {
                                if (prefix_inter_packet_time <= 210000) {
                                    if (hop_count <= 3500000) {
                                        if (inter_packet_time <= 210000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            /* Do nothing, go to next */
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 79500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 85000000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    }
                                } else {
                                    if (inter_packet_time <= 220000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (prefix_inter_packet_time <= 220000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                } else {
                    if (src_ipv6_addr <= 18500000) {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    } else {
                        if (new_addresses_seen <= 19500000) {
                            if (src_ipv6_addr <= 77500000) {
                                if (hop_count <= 4500000) {
                                    if (src_ipv6_addr <= 41500000) {
                                        if (new_addresses_seen <= 17500000) {
                                            if (src_ipv6_addr <= 24500000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                if (src_ipv6_addr <= 37000000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            if (prefix_inter_packet_time <= 230000) {
                                                if (src_ipv6_addr <= 38000000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (hop_count <= 5500000) {
                                        if (src_ipv6_addr <= 57500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 61500000) {
                                                if (new_addresses_seen <= 16500000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                }
                            } else {
                                if (hop_count <= 5500000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    if (src_ipv6_addr <= 102000000) {
                                        if (src_ipv6_addr <= 81500000) {
                                            if (hop_count <= 6500000) {
                                                if (prefix_inter_packet_time <= 230000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 98000000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (hop_count <= 6500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        }
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                }
                            }
                        } else {
                            if (src_ipv6_addr <= 62000000) {
                                if (src_ipv6_addr <= 57500000) {
                                    if (inter_packet_time <= 240000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (prefix_inter_packet_time <= 240000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                } else {
                                    if (hop_count <= 4500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        }
                    }
                }
            } else {
                if (new_addresses_seen <= 7500000) {
                    if (inter_packet_time <= 2830000) {
                        if (inter_packet_time <= 450000) {
                            if (hop_count <= 4500000) {
                                if (src_ipv6_addr <= 28000000) {
                                    /* Do nothing, go to next */
                                } else {
                                    if (hop_count <= 3500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                }
                            } else {
                                if (inter_packet_time <= 370000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    if (inter_packet_time <= 420000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                }
                            }
                        } else {
                            /* Do nothing, go to next */
                        }
                    } else {
                        // External Flooding Spoofing
                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                            external_flooding_spoofing_tcp_benign_drop();
                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                            external_flooding_spoofing_tcp_attack_drop();
                        } else {
                            external_flooding_spoofing_ns_drop();
                        }
                    }
                } else {
                    if (prefix_inter_packet_time <= 270000) {
                        if (src_ipv6_addr <= 98500000) {
                            if (new_addresses_seen <= 15500000) {
                                if (new_addresses_seen <= 14500000) {
                                    if (src_ipv6_addr <= 78000000) {
                                        if (hop_count <= 3500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            if (hop_count <= 4500000) {
                                                if (inter_packet_time <= 270000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 80500000) {
                                            /* Do nothing, go to next */
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                } else {
                                    if (inter_packet_time <= 260000) {
                                        if (inter_packet_time <= 250000) {
                                            if (src_ipv6_addr <= 68500000) {
                                                if (prefix_inter_packet_time <= 250000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                }
                            } else {
                                if (src_ipv6_addr <= 21000000) {
                                    if (hop_count <= 3500000) {
                                        /* Do nothing, go to next */
                                    } else {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    }
                                } else {
                                    if (src_ipv6_addr <= 58500000) {
                                        // External Flooding Spoofing
                                        if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                            external_flooding_spoofing_tcp_benign_drop();
                                        } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                            external_flooding_spoofing_tcp_attack_drop();
                                        } else {
                                            external_flooding_spoofing_ns_drop();
                                        }
                                    } else {
                                        if (src_ipv6_addr <= 61500000) {
                                            if (new_addresses_seen <= 19500000) {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            } else {
                                                if (hop_count <= 4500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    /* Do nothing, go to next */
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    }
                                }
                            }
                        } else {
                            if (hop_count <= 6500000) {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            } else {
                                if (src_ipv6_addr <= 102500000) {
                                    /* Do nothing, go to next */
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            }
                        }
                    } else {
                        if (new_addresses_seen <= 8500000) {
                            if (inter_packet_time <= 1760000) {
                                if (src_ipv6_addr <= 55000000) {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                } else {
                                    /* Do nothing, go to next */
                                }
                            } else {
                                // External Flooding Spoofing
                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                    external_flooding_spoofing_tcp_benign_drop();
                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                    external_flooding_spoofing_tcp_attack_drop();
                                } else {
                                    external_flooding_spoofing_ns_drop();
                                }
                            }
                        } else {
                            if (hop_count <= 4500000) {
                                if (src_ipv6_addr <= 39500000) {
                                    if (src_ipv6_addr <= 38500000) {
                                        if (new_addresses_seen <= 11000000) {
                                            if (prefix_inter_packet_time <= 4180000) {
                                                /* Do nothing, go to next */
                                            } else {
                                                // External Flooding Spoofing
                                                if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                    external_flooding_spoofing_tcp_benign_drop();
                                                } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                    external_flooding_spoofing_tcp_attack_drop();
                                                } else {
                                                    external_flooding_spoofing_ns_drop();
                                                }
                                            }
                                        } else {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            } else {
                                if (inter_packet_time <= 310000) {
                                    if (prefix_inter_packet_time <= 310000) {
                                        if (src_ipv6_addr <= 79500000) {
                                            // External Flooding Spoofing
                                            if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                external_flooding_spoofing_tcp_benign_drop();
                                            } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                external_flooding_spoofing_tcp_attack_drop();
                                            } else {
                                                external_flooding_spoofing_ns_drop();
                                            }
                                        } else {
                                            if (src_ipv6_addr <= 80500000) {
                                                if (inter_packet_time <= 300000) {
                                                    /* Do nothing, go to next */
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            } else {
                                                if (src_ipv6_addr <= 98500000) {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                } else {
                                                    // External Flooding Spoofing
                                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                                        external_flooding_spoofing_tcp_benign_drop();
                                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                                        external_flooding_spoofing_tcp_attack_drop();
                                                    } else {
                                                        external_flooding_spoofing_ns_drop();
                                                    }
                                                }
                                            }
                                        }
                                    } else {
                                        /* Do nothing, go to next */
                                    }
                                } else {
                                    // External Flooding Spoofing
                                    if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal == 1000) {
                                        external_flooding_spoofing_tcp_benign_drop();
                                    } else if (hdr.tcp.isValid() && hdr.tcp_option_ts.isValid() && hdr.tcp_option_ts.tsVal != 1000) {
                                        external_flooding_spoofing_tcp_attack_drop();
                                    } else {
                                        external_flooding_spoofing_ns_drop();
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}